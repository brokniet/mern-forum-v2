import Post from "../models/Post.js";

export async function getPostsByTitle(req, res) {
  // const title = req.query.title;
  // try {
  //   const posts = await Post.find(); //rompe anashent
  //   res.json(posts);
  // } catch (error) {
  //   res.json(error);
  // }
}
